spring.application.name=hr-oauth
spring.cloud.config.uri=http://localhost:8888

#spring.profiles.active=test

#allows that actuator update the configs in runtime
management.endpoints.web.exposure.include=*